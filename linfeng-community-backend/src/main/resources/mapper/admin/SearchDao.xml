<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.linfeng.modules.admin.dao.SearchDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.linfeng.modules.admin.entity.SearchEntity" id="searchMap">
        <result property="searchId" column="search_id"/>
        <result property="uid" column="uid"/>
        <result property="content" column="content"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>


    <select id="selectHotSearch" resultType="java.lang.String">
      select content from lf_search group by content
      order by count(content) desc
      limit 10
    </select>

</mapper>