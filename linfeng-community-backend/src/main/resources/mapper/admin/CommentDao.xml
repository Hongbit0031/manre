<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.linfeng.modules.admin.dao.CommentDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.linfeng.modules.admin.entity.CommentEntity" id="commentMap">
        <result property="id" column="id"/>
        <result property="pid" column="pid"/>
        <result property="type" column="type"/>
        <result property="uid" column="uid"/>
        <result property="toUid" column="to_uid"/>
        <result property="postId" column="post_id"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="img" column="img"/>
    </resultMap>

    <select id="getAllCountByPostId" parameterType="java.util.List" resultType="io.linfeng.common.vo.app.CommentCountResponse">
        SELECT IFNULL(COUNT(*),0) AS number,post_id AS postId FROM lf_comment WHERE status=1 and post_id IN
        <foreach collection="list" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
        GROUP BY post_id ORDER BY FIELD(post_id,
        <foreach collection="list" item="postId" separator="," close=")">
            #{postId}
        </foreach>
    </select>
</mapper>